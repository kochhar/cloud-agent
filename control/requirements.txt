# Install with:
#   .venv/bin/pip install -r requirements.txt

fastapi>=0.115
uvicorn>=0.30
pydantic>=2.7
psycopg[binary,pool]>=3.2
httpx>=0.27

# Bundled PostgreSQL binaries; see scripts/postgres.sh
pgserver==0.1.4
